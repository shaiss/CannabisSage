# Legacy Tampermonkey userscripts

These files are the original proof-of-concept userscripts. They are **not** the supported install path.

Use the Manifest V3 Chrome extension in [`../extension/`](../extension/) instead.

| File | Notes |
| --- | --- |
| `sunnyside-insights.user.js` | Full POC (hover + compare) using `GM_xmlhttpRequest` |
| `test-script.user.js` | Minimal Tampermonkey load check |
